"""Creator Profile bounded context.

The Memory slice publishes immutable domain behavior and closed command
orchestration.  PostgreSQL, HTTP, and production composition remain separate
planned slices.
"""
