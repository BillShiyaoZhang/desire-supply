"""Canonical, versioned API, event, domain, and runtime contracts.

Read these package resources through ``importlib.resources`` so the same code
works from a source checkout, wheel, sdist-built wheel, or zip importer.
"""
